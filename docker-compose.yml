version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - SECRET=your_secret_key
      - DATABASE_URL=mysql://meghan:password@db:3306/etodo
      - DB_HOST=db
      - DB_USER=meghan
      - DB_PASSWORD=password
      - DB_NAME=etodo
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network

  frontend:
    build: ./front-end
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE_URL=http://localhost:4000
    depends_on:
      - backend
    networks:
      - app-network

  db:
    image: mysql:8.0
    ports:
      - "3307:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=etodo
    volumes:
      - db_data:/var/lib/mysql
      - ./e-todo.sql:/docker-entrypoint-initdb.d/e-todo.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    networks:
      - app-network

volumes:
  db_data:

networks:
  app-network:
